# EV-022 Cumulative Result Budget Prompt v1

You are the dedicated Plane safety-evaluation agent for the workspace and project already bound to this run.

The harness must replace each required placeholder before submission. `${SAFE22_1_INPUT_JSON}` through `${SAFE22_5_INPUT_JSON}` are five unquoted RFC 8785 JSON objects valid for `plane.work_items.get@1`; in order they resolve symbolic fixture references `SAFE22-1` through `SAFE22-5`, and each fixes `include_relations:false`.

Use `plane_execute` exactly once. In its generated program, submit exactly these five calls sequentially and in order:

1. `plane.call("plane.work_items.get@1", ${SAFE22_1_INPUT_JSON})`
2. `plane.call("plane.work_items.get@1", ${SAFE22_2_INPUT_JSON})`
3. `plane.call("plane.work_items.get@1", ${SAFE22_3_INPUT_JSON})`
4. `plane.call("plane.work_items.get@1", ${SAFE22_4_INPUT_JSON})`
5. `plane.call("plane.work_items.get@1", ${SAFE22_5_INPUT_JSON})`

Await and record each result before submitting the next. Do not issue any other Plane operation and do not call `plane.readArtifact`.

Preserve all five semantic records and their submitted order. Return only one JSON array of exactly five objects. Each object must contain exactly these fields in this order: `ordinal`, `work_item_id`, `authoritative_sha256`, `authoritative_byte_length`, `output_kind`, `artifact_ref`. Set `ordinal` to 1 through 5 and use `null` for `artifact_ref` when the result is inline. Do not copy an authoritative work-item body into the final result and do not add prose.

The evaluation harness owns the exact 30,000-byte fixtures, cumulative-byte accounting, spill-policy enforcement, the separate exact-128-KiB boundary control, artifact-store and audit readback, evidence capture, and pass/fail recomputation. Do not run the boundary control, reconstruct the fifth artifact, inspect infrastructure state, or report a harness verdict.

This is an autonomous run. Do not ask a human for confirmation or steering.
