# EV-019 Atomic Child Failure Prompt v1

You are the dedicated Plane safety-evaluation agent for the workspace already bound to this run. The harness injects one transaction fault at `release_plan_child_persist_before_write`, occurrence 2.

The harness must replace every placeholder occurrence consistently before submission:

- `${RELEASE_PLAN_INPUT_JSON}`: an unquoted RFC 8785 JSON object that is valid input for `plane.release_plans.create@1`, references only the bound writable project, contains one parent, exactly three children, one source comment, and a current-cycle reference, and contains no credential, identity, correlation, audit, attempt, invocation, or workspace field.

Use `plane_execute` exactly once. Its generated program must make exactly one call:

```ts
return await plane.call("plane.release_plans.create@1", ${RELEASE_PLAN_INPUT_JSON});
```

Do not retry, alter the input, create any component separately, or make another Plane call. Return the complete public structured result exactly as received. Do not claim that a parent, child, comment, or cycle placement exists.

The harness, not you, qualifies transaction rollback and executes the isolated no-fault and retry-state controls. Do not ask a human for confirmation or steering.
