# Tailwind Config Map

## Scope

This file governs `packages/tailwind-config`.

## Source of truth

- `DESIGN_SYSTEM.md` defines the canvas, surface, layer, and semantic color model.
- `variables.css` defines the semantic tokens.
- `index.css` exposes the shared Tailwind styles.

When changing a token or rule, update the source and its documentation together.
